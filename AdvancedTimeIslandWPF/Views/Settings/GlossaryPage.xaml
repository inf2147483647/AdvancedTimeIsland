<UserControl x:Class="AdvancedTimeIsland.Views.Settings.GlossaryPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ci="http://classisland.tech/schemas/xaml/core"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             Background="{DynamicResource MaterialDesignPaper}"
             FontFamily="{DynamicResource HarmonyOsSans}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="14"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto">
    <ScrollViewer>
        <StackPanel Style="{DynamicResource SettingsPageStackPanelStyle}">
            <ci:IconText Kind="BookOpenVariant" Text="专业名词解释" Margin="0 0 0 8" />

            <materialDesign:Card Margin="0 0 0 6">
                <StackPanel x:Name="ContentPanel" Margin="16" />
            </materialDesign:Card>
        </StackPanel>
    </ScrollViewer>
</UserControl>
